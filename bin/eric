#!/bin/sh
# eric-lang CLI wrapper
# "pip install eric-lang" → "julia --project src/cli.jl"
# Progress.

SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
exec julia --project="$SCRIPT_DIR" "$SCRIPT_DIR/src/cli.jl" "$@"
